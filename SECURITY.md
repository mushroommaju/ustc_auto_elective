# 安全与隐私

这是非官方工具。只在学校允许自动化、本人有权操作的账户与选课批次中使用。

## 不应公开的文件

`config.ini`、`.local/`、Cookie、完整选课 URL、会话 JSON、事务日志、个人课表、
HAR、认证站 HTML/响应正文、截图、浏览器配置及账号密码都不应上传。
会话文件虽然不含明文密码，仍可用于访问账户，应视为凭据；本工具仅限制保存范围，
不对磁盘上的 Cookie 加密。请使用受控的本机账户与磁盘权限。

`.gitignore` 不能移除已经提交到 Git 的秘密。若发生泄露：先在学校提供的账户安全入口
退出相关会话/重置凭据，再清理 Git 历史和公开附件。删除最新版本中的文件不等于撤销凭据。

## 提交问题前

- 仅提供版本、操作系统、错误分类及经过人工核对的最小复现。
- 不粘贴完整异常、HAR、Cookie、认证正文或真实学生编号。
- 测试使用虚构数据，不以其他同学账户或实际选退课作为公开 CI 测试。
- 发现认证泄露问题，请通过维护者提供的私密联系渠道报告；未提供渠道时，不要在公开 Issue 上传敏感材料。

## 自动操作边界

选退课不是原子事务，恢复旧课不保证成功。未知结果时写入保护日志并停写，
不得删除日志来盲目重试。先人工核对官网，确认没有仍在处理的请求，参见 README 的恢复步骤。
同一账户不得在多个目录、扩展或其他程序中同时自动选退课；进程锁只覆盖当前项目目录。

不提供代理/IP 轮换、验证码绕过、身份伪装、封禁绕过或多账户并发。
限流时等待，登录失效时使用官网验证流程，不将这些保护当作需要规避的障碍。
